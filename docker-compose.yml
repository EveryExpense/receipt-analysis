services:
  ocr-api:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    depends_on:
      - llm-server

  llm-server:
    build:
      context: .
      dockerfile: Dockerfile.llm